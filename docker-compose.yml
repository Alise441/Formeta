services:
  bot:
    build: .
    env_file: .env
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/formeta.db
      - ANKI_OUTPUT_DIR=/app/data/exports
    restart: unless-stopped
